import type { ReactNode } from "react";

export const metadata = {
  title: "Modes & subscription plans | Neo AI",
  description:
    "Personal Assistant, Life Coach, Learning, and Market Analyst modes. Subscription plan pricing (configurable by admin).",
};

export default function PersonalAssistantLayout({ children }: { children: ReactNode }) {
  return children;
}
